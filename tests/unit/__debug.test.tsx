// Leftover scratch file from debugging a timezone bug in the Scheduling
// Overview calendar during Phase 63 — this sandbox can't unlink files once
// they're synced into the project folder, so it can't be deleted from here.
// Not meant to ship: remove via `git rm tests/unit/__debug.test.tsx` as part
// of this phase's commit (see the git command block in EXECUTION_PLAN.md).
it("is a placeholder pending deletion", () => {
  expect(true).toBe(true);
});
